//

import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            FeatureMenuView()
        }
        .lumenotePalette()
    }
}

#Preview {
    ContentView()
}
